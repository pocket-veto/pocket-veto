package io.pocketveto.ui.theme

import androidx.compose.ui.graphics.Color

// PocketVeto palette — matches the res/values/colors.xml entry points so a
// future XML theme migration can reuse the same hex values.
val PvBlue = Color(0xFF1E6FD9)
val PvGreen = Color(0xFF1F9D55)
val PvRed = Color(0xFFD12B2B)
val PvAmber = Color(0xFFE0A800)
val PvBg = Color(0xFF101418)
val PvSurface = Color(0xFF1B2127)
val PvOnSurface = Color(0xFFE6EDF3)
val PvOnSurfaceVariant = Color(0xFFB6C2CF)
val PvOutline = Color(0xFF3A434D)

// Light scheme (used when system theme is light).
val PvLightBg = Color(0xFFF6F8FA)
val PvLightSurface = Color(0xFFFFFFFF)
val PvLightOnSurface = Color(0xFF1B2127)
val PvLightOnSurfaceVariant = Color(0xFF444C56)
val PvLightOutline = Color(0xFFC8D1DA)
